fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
pandas==2.1.4
openpyxl==3.1.2
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0
streamlit==1.30.0
plotly==5.18.0
httpx==0.26.0
pytest==7.4.4
pyarrow==14.0.2
scipy==1.11.4
statsmodels>=0.14.0
# Phase 2: auth + RBAC
pyjwt==2.9.0
passlib==1.7.4
bcrypt==4.2.0
itsdangerous==2.2.0

# Phase 21: optional Bedrock provider for AI briefing.
# Comment out if you're not using BRIEFING_PROVIDER=bedrock — the provider
# degrades gracefully when boto3 is missing.
boto3>=1.34.0

# Pricing Studio Phase A6: APScheduler for the scheduled-publish runner +
# follow-on alert/lineage crons (Phase J).
apscheduler>=3.10.0

# Pricing Studio Phase A8: file-watch hot reload for the approval rules JSON.
# Rules engine degrades gracefully when watchdog is missing (no hot reload,
# but startup file load + DB fallback still work).
watchdog>=3.0.0
